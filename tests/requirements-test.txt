# DEPRECATED: This file is no longer used
# OpsZen now uses pyproject.toml with uv for dependency management

# To install test dependencies, use one of the following:

# Option 1: Using uv (recommended)
# uv pip install -e ".[dev]"

# Option 2: Using make
# make install-dev

# Option 3: Using pip with pyproject.toml
# pip install -e ".[dev]"

# All test dependencies are now defined in pyproject.toml under [dependency-groups]
# See pyproject.toml for the complete list of development dependencies

# Quick start:
# 1. Install uv: curl -LsSf https://astral.sh/uv/install.sh | sh
# 2. Create venv: uv venv .venv
# 3. Activate: source .venv/bin/activate (Unix) or .venv\Scripts\activate (Windows)
# 4. Install: uv pip install -e ".[dev]"
# 5. Run tests: make test

# For more information, see:
# - README.md (Development Setup section)
# - docs/CI_CD.md (CI/CD documentation)
# - pyproject.toml (Dependency definitions)
